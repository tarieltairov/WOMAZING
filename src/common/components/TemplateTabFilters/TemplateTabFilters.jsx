import styles from './TemplateTabFilters.module.scss'

export function TemplateTabFilters({ items = [], active, onChange }) {
  return (
    <div className={styles.container}>
      {/* перебираете items */}
      {/* onChange(item.value) */}
    </div>
  )
}
